!function(e, i, t) {
    "use strict";
    t(i).ready(function() {
        function a(e, i) {
            e.children(".submenu-content").show().slideUp(200, function() {
                t(this).css("display", "");
                t(this).find(".menu-item").removeClass("is-shown");
                e.removeClass("open");
                i && i();
            });
        }

        var s = t(".app-sidebar"),
            n = t(".sidebar-content"),
            l = t(".wrapper"),
            o = i.querySelector(".sidebar-content");

        // Check if `o` exists before initializing PerfectScrollbar
        if (o) {
            new PerfectScrollbar(o, {
                wheelSpeed: 10,
                wheelPropagation: true,
                minScrollbarLength: 5,
            });
        } else {
            console.warn("Sidebar content element not found. PerfectScrollbar not initialized.");
        }

        n.on("click", ".navigation-main .nav-item a", function() {
            var e = t(this).parent(".nav-item");
            if (e.hasClass("has-sub") && e.hasClass("open")) a(e);
            else {
                if (e.hasClass("has-sub") && function(e, i) {
                        var a = e.children(".submenu-content"),
                            s = a.children(".menu-item").addClass("is-hidden");
                        e.addClass("open");
                        a.hide().slideDown(200, function() {
                            t(this).css("display", "");
                        });
                        setTimeout(function() {
                            s.addClass("is-shown").removeClass("is-hidden");
                        }, 0);
                    }(e), n.data("collapsible")) return !1;
                a(e.siblings(".open"));
                e.siblings(".open").find(".nav-item.open").removeClass("open");
            }
        });

        t(".nav-toggle").on("click", function() {
            var e = t(this).find(".toggle-icon");
            if ("expanded" === e.attr("data-toggle")) {
                l.addClass("nav-collapsed");
                t(".nav-toggle").find(".toggle-icon").removeClass("ik-toggle-right").addClass("ik-toggle-left");
                e.attr("data-toggle", "collapsed");
            } else {
                l.removeClass("nav-collapsed menu-collapsed");
                t(".nav-toggle").find(".toggle-icon").removeClass("ik-toggle-left").addClass("ik-toggle-right");
                e.attr("data-toggle", "expanded");
            }
        });

        s.on("mouseenter", function() {
            if (l.hasClass("nav-collapsed")) {
                l.removeClass("menu-collapsed");
                var e = t(".navigation-main .nav-item.nav-collapsed-open");
                e.children(".submenu-content").hide().slideDown(300, function() {
                    t(this).css("display", "");
                });
                n.find(".nav-item.active").parents(".nav-item").addClass("open");
                e.addClass("open").removeClass("nav-collapsed-open");
            }
        }).on("mouseleave", function(e) {
            if (l.hasClass("nav-collapsed")) {
                l.addClass("menu-collapsed");
                var i = t(".navigation-main .nav-item.open"),
                    a = i.children(".submenu-content");
                i.addClass("nav-collapsed-open");
                a.show().slideUp(300, function() {
                    t(this).css("display", "");
                });
                i.removeClass("open");
            }
        });

        t(e).width() < 992 && (s.addClass("hide-sidebar"), l.removeClass("nav-collapsed menu-collapsed"));

        t(e).resize(function() {
            t(e).width() < 992 && (s.addClass("hide-sidebar"), l.removeClass("nav-collapsed menu-collapsed"));
            t(e).width() > 992 && (s.removeClass("hide-sidebar"), "collapsed" === t(".toggle-icon").attr("data-toggle") && l.not(".nav-collapsed menu-collapsed") && l.addClass("nav-collapsed menu-collapsed"));
        });

        // Other event listeners for functionality
        t(i).on("click", ".navigation li:not(.has-sub)", function() {
            t(e).width() < 992 && s.addClass("hide-sidebar");
        });

        t(i).on("click", ".logo-text", function() {
            t(e).width() < 992 && s.addClass("hide-sidebar");
        });

        t(".mobile-nav-toggle").on("click", function(e) {
            e.stopPropagation();
            s.toggleClass("hide-sidebar");
        });

        t("html").on("click", function(i) {
            t(e).width() < 992 && (s.hasClass("hide-sidebar") || 0 !== s.has(i.target).length || s.addClass("hide-sidebar"));
        });

        t("#sidebarClose").on("click", function() {
            s.addClass("hide-sidebar");
        });

        t('[data-toggle="tooltip"]').tooltip();

        // More custom behavior below (checkbox, search, and widget interactions)
    });
}(window, document, jQuery);
